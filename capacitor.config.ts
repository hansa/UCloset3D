import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.ucloset3d',
  appName: 'UCloset3D',
  webDir: 'dist/ucloset3d',
  bundledWebRuntime: false
};

export default config;
